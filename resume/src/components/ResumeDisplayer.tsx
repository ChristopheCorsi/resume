import React, { ReactElement } from 'react'
import { ResumeObject } from '../types/resume'

interface Props {
    resumeObject: ResumeObject;
}

export default function ResumeDisplayer(props:Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
